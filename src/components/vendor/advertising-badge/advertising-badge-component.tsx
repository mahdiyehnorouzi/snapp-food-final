import React, { FC } from "react";
import styles from "./styles.module.scss";

export const AdvertisingBadgeComponent: FC = () => {
  return <span className={styles.coupon__Container}>آگهی</span>;
};
